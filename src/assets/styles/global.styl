html, body{
  margin 0
  padding 0
  width 100%
  height 100%
}
body{
  background-image: url('../images/bg.jpeg')

  // 将背景图片等比缩放到完全覆盖容器, 有可能超出容器
  background-size: cover
  background-position: center;
  font: 14px 'Helvetica Neue', Helvetica, Arial, sans-serif;
  color: #4d4d4d;
  font-smoothing: antialiased;
  font-weight: 300;
}